---
description: 'A node app example that uses Express'
labels: ['application', 'node', 'app', 'express', 'demo']
---

Run the following to make this app loadable by your workspace:

```bash
bit use composeland.apps/server
```

Run the following to run this app:

```bash
bit run server
```
